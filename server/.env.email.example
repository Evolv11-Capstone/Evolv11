# Email Service Configuration
# Add ONE of the following email provider configurations to your .env file

# Option 1: Postmark (Recommended for transactional emails)
# POSTMARK_API_KEY=your_postmark_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# Option 2: SendGrid
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# Option 3: AWS SES
# AWS_SES_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# FROM_EMAIL=noreply@yourdomain.com

# Application Configuration
APP_NAME=Clean Evolution

# Email Service Setup Instructions:

# POSTMARK SETUP:
# 1. Sign up at https://postmarkapp.com/
# 2. Create a server and get your API key
# 3. Verify your sender domain or email
# 4. Add POSTMARK_API_KEY to your .env file

# SENDGRID SETUP:
# 1. Sign up at https://sendgrid.com/
# 2. Create an API key with Mail Send permissions
# 3. Verify your sender domain or email
# 4. Add SENDGRID_API_KEY to your .env file

# AWS SES SETUP:
# 1. Configure AWS credentials (IAM user with SES permissions)
# 2. Verify your sender domain or email in AWS SES
# 3. Add AWS_SES_REGION and credentials to your .env file

# Note: If no email provider is configured, the service will log a message
# and continue without sending emails (graceful degradation).
